# 2026-06-05

## start_infoflow_web.ps1 重构
- **删除隧道 HTTP 验证循环**：验证根本解决不了 trycloudflare 路由问题（metrics 显示 tunnel_total_requests=0，外部 WebFetch 一样失败），只导致 3×8×2s=48s 无意义等待
- 隧道创建改为简单的 URL 匹配 + 3s 静置，匹配到就用
- cloudflared 添加 `--edge-ip-version 4` 强制 IPv4 路由
- 清理未使用变量（$chromeExe, $chromeScript, $pyExe）
- 脚本从 188 行精简到 205 行（验证逻辑移除、结构更扁平）
