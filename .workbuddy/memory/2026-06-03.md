# 2026-06-03

## start_infoflow_web.ps1 修复（多轮迭代）
- 移除 `Start-Process` 的 `-Environment` 参数（PS 5.1 不支持）
- cloudflared 从 2026.5.0 → 2026.5.2
- `--protocol auto` → `--protocol http2`：QUIC 隧道注册成功但不转发请求（tunnel_total_requests=0），HTTP/2 稳定
- Phase 4 改为默认浏览器打开
- Phase 3b 隧道验证改为 WebClient + 最多 8 次 2s 间隔重试，避免 PS 5.1 的 Invoke-WebRequest SSL 问题
- 脚本顶部加入 `Tls12` 强制声明
