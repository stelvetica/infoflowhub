# 2026-06-03

## start_infoflow_web.ps1 修复
- 移除 `Start-Process` 的 `-Environment` 参数（PS 5.1 不支持，仅 PS 7+ 可用）
- cloudflared 从 2026.5.0 更新到 2026.5.2：旧版隧道注册成功但零请求通过，新版自带 connectivity pre-checks
- cloudflared 参数加入 `--protocol auto`
- Phase 4 改为默认浏览器打开（不再硬编码 Chrome 路径）
- Phase 3b 隧道创建增加重试机制（最多 3 次），每次获取 URL 后实际 HTTP 验证，避免 ERR_CONNECTION_CLOSED 的假成功
