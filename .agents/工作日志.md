# 工作日志

- 需求：完成 Next.js 迁移闭环、清乱码、退役旧页面服务、清理提交噪音并推送。
- 进度：页面、README、健康状态中文已统一；旧 `web/server.py` 已退场；运行期文件已从版本库索引移除。
- 验证：需完成 build、启动链路、`fetch-now` 和页面桥接联调后推送。
- 风险：抓取器相关改动并非本次任务产生，提交时需避免误带用户现有脏改动。
2026-05-11
- 需求：核对“稍后处理”界面条目数仅显示 500 的问题。
- 排查：确认 `scripts/web_bridge.py` 对 laterhub 与 entries 列表都写死 `LIMIT 500`，前端直接用 `rows.length` 作为卡片统计。
- 处理：桥接层新增总数字段 `laterhub_total`、`entries_total`；前端卡片改读真实总数，列表仍保留 500 条上限。
- 验证：`laterhub-snapshot` 返回 `laterhub_total=1179`，`entries-snapshot` 返回 `entries_total=276`。
2026-05-11
- 需求：排查 `main` 推送失败。
- 结论：失败原因为带 lease 的覆盖推送使用了过期预期值 `a0155e1`，远端实际已在 `472f0a6`。
- 处理：改用普通推送，已成功更新远端 `main` 到 `b1d3e09`。
2026-05-11
- 需求：修正订阅列表时间取值、排序与单行展示。
- 排查：B站动态部分 `published` 使用模糊文案兜底，旧数据存在大量 `00:00/00:01`；前端表格未做单行截断。
- 处理：B站接口优先写入真实发布时间戳；前端统一解析时间并按其排序；来源和标题改为固定宽度单行省略；补充历史修复脚本。
- 验证：`npm run build` 通过，开发页返回 200。
2026-05-11
- 需求：删除订阅数据库并全量重抓，同时过滤微博置顶。
- 处理：RSS 与网页源统一限制为每源最多 20 条；微博解析阶段跳过置顶内容；删除 `data/subscriptions.sqlite3` 后执行全量抓取。
- 结果：重建后共 242 条、16 个源；微博置顶命中为 0；HackerNews 因 SSL EOF 本轮抓取失败。
