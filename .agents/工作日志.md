## 2026-05-13 MacroMargin 登录态预检
- 需求：为需本机登录态的网页源补预检与专门提示，先覆盖 MacroMargin。
- 改动：设置页新增“登录态要求”列；MacroMargin 标注“依赖本机 Chrome Profile 2 登录态”；X 抓取前先检查 Profile 2 目录与关键文件；失败时返回可执行提示。
- 验证：`npm run build` 通过。

## 2026-05-13 订阅方向分析
- 需求：分析当前订阅 up 主要方向，并判断其是否可视作新闻流。
- 结果：共 21 个源，启用 18 个；核心集中在观点/宏观/外资观点/技术，整体更接近“解释型新闻 + 研究型资讯”而非泛娱乐订阅。
- 备注：新闻属性成立，但应区分硬新闻、观点评论、专题研究、工具技术更新。

## 2026-05-13 首页渲染异常修复
- 现象：首页出现整块异常底色、乱码文案、局部结构错乱。
- 根因：前端页面与组件存在编码损坏，部分 JSX 文本和标签被破坏。
- 处理：重写 `app/page.tsx`、`app/layout.tsx` 与 4 个前端组件的受损片段，保持原逻辑不变。
- 验证：`npm run build` 通过；本地 `http://localhost:18421/` 实测渲染恢复正常。

## 2026-05-13 订阅源弹窗编辑
- 需求：将“新增订阅源”改为订阅源管理首行按钮；新增与编辑统一改为居中弹窗。
- 处理：新增 `app/components/sources-settings-client.tsx`；设置页接入弹窗式新增/编辑；补充弹窗样式。
- 验证：`npm run build` 通过；本地开发页重启后可用，浏览器桥接复测时出现超时，未影响构建结果。

## 2026-05-13 Python Web + HTMX 迁移
- 目标：去掉 `.next` / HMR / Node-Python bridge，改为 Python 单栈 Web 控制台。
- 处理：新增 `web/` 下 FastAPI、Jinja2、HTMX 页面与片段；新增 `requirements-web.txt`；新增 Python Web 启动脚本并将旧启动脚本重定向到新链路；README 改为 Python 单栈说明。
- 验证：已安装 `fastapi`、`uvicorn` 等依赖；新服务已在 `http://127.0.0.1:18421/` 启动；浏览器实际截图确认首页和设置页可渲染。

## 2026-05-13 scripts 目录清理
- 目标：保留 Python Web 启动链路与辅助工具，删除旧 Next / bridge 残留。
- 处理：删除 `web_bridge.py`、`start_infoflow.ps1`、`restart_infoflow_dev.ps1`、旧 bat；新增 `一键重新拉起InfoFlowHub服务.bat`；README 与 `package.json` 同步改为 Python Web 启动入口。
- 验证：`scripts/` 当前仅保留 Web 启动/重启、登录辅助、B 站时间修复和新的双击重启脚本。

## 2026-05-13 订阅名称统一
- 需求：按 `技术 / 观点 / 信息` 统一订阅名称；仅 `星话大白` 归为 `信息`。
- 处理：更新主配置与运行时别名映射；将未带 `观点` 的相关观点源统一补齐为 `观点` 前缀。
- 结果：显示名称已统一，旧分类词仅残留在部分 `id`，不影响使用。

## 2026-05-13 内部标识清理
- 需求：内部 `id` 改为原始 up 名/源名，不再携带分类前缀。
- 处理：按订阅源地址重写 `config/rss_sources.json` 的 `id`，并同步迁移订阅库与健康状态中的 `source_id`。
- 结果：当前订阅源已切换为简洁标识，如 `labuladong`、`MacroMargin`、`星话大白`、`土豆看财报`。

## 2026-05-13 历史脏键清理
- 需求：清空历史遗留的无效健康状态键与启停状态键。
- 处理：仅保留当前订阅集对应的健康状态；删除数据库 `rss_source_state` 中不再属于当前配置的旧 `source_id`。
- 结果：历史残留已清空，当前有效订阅源之外的状态键为 0。
