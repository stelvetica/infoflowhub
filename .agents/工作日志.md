## 项目总需求与整体计划
- 目标：持续打磨 InfoFlowHub 的订阅与稍后处理后台，优先修正影响使用效率的界面与流程问题。
- 计划：按评审意见逐项修正，验证页面表现，保持改动最小且可回退。

## 2026-05-13 设置页布局与操作按钮优化
- 进度：将“稍后处理概览”上移到设置页顶部；订阅源操作改为同一行；启用/停用使用区分色。
- 难点与方案：桌面端强制横排，移动端再回落换行，避免操作列挤压错位。

## 2026-05-13 订阅源编辑保存修复
- 进度：修复预设别名覆盖已编辑订阅源，保存后可保留用户修改。
- 难点与方案：将别名规则改为仅补空字段，避免页面重载回写默认值。

## 2026-05-13 订阅源主数据与渠道列
- 进度：编辑订阅源后，名称与 feed/site URL 作为主数据复用到抓取与展示；设置页新增渠道列。
- 难点与方案：保存时按 URL 重算 provider/fetch_via/kind，避免展示值与抓取逻辑脱节。

## 2026-05-13 渠道列接入 connector 识别
- 进度：渠道列改为复用 connector 侧站点识别，不再由页面层自行猜测。
- 难点与方案：统一走 resolve_web_target，后续新增站点只需在 connector 识别层补一次。

## 2026-05-13 重启脚本修复
- 进度：启动脚本改为先按 18421 端口清理旧进程，再等待端口释放后启动新服务。
- 难点与方案：避开 PowerShell 只读变量名，统一使用端口级兜底，解决假重启问题。

## 2026-05-13 订阅源序号列
- 进度：订阅源管理新增序号列，按当前列表顺序显示 1、2、3。

## 2026-05-13 缺失订阅源恢复
- 需求：仅恢复历史配置中缺失的 9 个订阅源。
- 处理：从历史 `config/rss_sources.json` 回补到当前配置，保持现有分类口径与启停状态。
- 结果：当前订阅源数量已从 12 恢复到 21。

## 2026-05-13 订阅源去重核验
- 需求：确认恢复后无重复源、无脏别名冲突。
- 处理：按 `id / name / feed_url / site_url` 四个维度核验去重。
- 结果：当前 21 个源四项均无重复，可继续使用。

## 2026-05-13 健康状态键补齐
- 需求：将 `runtime/health/subscriptions_source_health.json` 的旧键迁移到当前 21 个订阅源 `id`。
- 处理：按 `feed_url / site_url` 匹配旧状态并重建健康状态键；未命中的新源补空状态。
- 结果：健康状态键已与当前配置完全对齐，缺失与冗余均为 0。

## 2026-05-13 一键重启脚本修复
- 需求：确认 `一键重新拉起InfoFlowHub服务.bat` 是否仍可用。
- 处理：修正批处理中的脚本相对路径，由 `scripts\\restart_infoflow_web.ps1` 改为同目录 `restart_infoflow_web.ps1`。
- 结果：批处理已可正常执行，不再因路径错误失效。

## 2026-05-13 订阅名称最终对齐
- 需求：按用户提供的 21 条最终名称统一订阅名称。
- 处理：更新主配置与运行时别名映射，避免页面刷新后回退旧名称。
- 结果：当前订阅名称已与用户给定版本一致。

## 2026-05-13 订阅源列表列宽收紧
- 进度：为订阅源表设置列宽；名称/渠道/来源类型/登录要求/最近错误统一省略显示，悬停可看完整值。

## 2026-05-13 渠道列排序
- 进度：渠道列接入与名称列一致的升降序排序。

## 2026-05-13 订阅抓取修复
- 进度：补上 `apps.subscriptions.rss_pipeline` 命令行入口，修复空跑与中文输出乱码；复跑后抓取恢复。
- 难点与方案：失败项分层处理；2 个 YouTube 源已确认 feed 404 并停用，`MacroMargin` 改为登录依赖提示，不再把整轮抓取判为系统异常。

## 2026-05-13 YouTube 兜底直抓
- 进度：为 YouTube 频道增加“保留 RSS，失败后网页直抓兜底”；`老厉害` 已验证可抓 12 条。
- 难点与方案：RSS 仍为主路径，仅对 YouTube 失败源回退；页面解析改为 `ytInitialData` + 文本卡片双兜底，降低频道页结构波动影响。

## 2026-05-13 MacroMargin 登录态误判修复
- 进度：兼容新版 Chrome `Profile 2/Network/Cookies` 路径，消除登录态误判；MacroMargin 已恢复抓取 12 条。
- 难点与方案：保留旧路径兼容，同时增加新路径探测，避免 Chrome 版本变化导致 X 抓取失效。

## 2026-05-13 Web 立即抓取事件循环修复
- 进度：修复设置页“立即抓取”在 FastAPI 异步路由中直接调用同步抓取器，改为线程池执行；页面按钮已验证恢复正常。
- 难点与方案：保持现有同步抓取实现不动，只在 Web 入口隔离到线程池，最小改动消除 Playwright Sync API 与 asyncio 冲突。

## 2026-05-13 订阅源主数据收敛
- 进度：将 `config/rss_sources.json` 固化为唯一主数据源；补齐 `channel/auth_type/auth_profile/fallback_mode`；停止视图层回写配置、停止 DB 覆盖 `enabled`、健康状态仅保留运行态字段。
- 难点与方案：按最小风险先收敛 Python 主链路，保留旧 Next 残留不再参与当前 Web 主流程，避免双实现继续污染主数据。

## 2026-05-13 旧 Next 清理完成
- 进度：删除 `app/`、`lib/`、`next.config.ts`、`next-env.d.ts`、`package*.json`、`tsconfig.json` 等旧前端残留；仓库说明同步为 Python Web 单前台。

## 2026-05-15 首页双列表分页条数固定
- 需求：将首页左侧订阅内容每页固定为 20 条，右侧稍后处理每页固定为 10 条。
- 处理：直接修改 `web/services/views.py` 中两处分页常量，改为后端写死，避免仅改前端展示导致实际分页不一致。

## 2026-05-14 Alpha派蓝宝书入口更新
- 进度：首页“Alpha派 蓝宝书”跳转已改为 `https://alphapai-web.rabyte.cn/reading/home/my-focus`。

## 2026-05-14 抖音订阅发布时间重刷校验
- 需求：修正 3 个抖音订阅源页面中错误的抓取时间，改为真实发布时间。
- 处理：对 `破壁狗 / 江平舟 / 黑猫星球` 逐个删除旧库记录后重抓重入库，并按数据库 `published/published_at` 结果做结构化校验。
- 结果：3 个源各重刷 12 条，库内时间已更新为真实发布时间；例如 `破壁狗` 最新为 `2026/05/10 16:35`，`江平舟` 最新为 `2026/05/11 11:09`，`黑猫星球` 最新为 `2026/05/13 13:12`。

## 2026-05-14 B站订阅条目数偏少排查
- 需求：确认“至少 10 条且覆盖近 3 天”的抓取规则是否仍生效，并分析部分 B 站源为何页面仅显示 `1~3` 条。
- 处理：核验主抓取器阈值仍为 `MIN_SOURCE_ENTRIES = 10` 与 `MIN_SOURCE_DAYS = 3`；对异常 B 站源逐个直抓验证，并执行定向删库重刷。
- 结果：规则仍生效；“仅 1~3 条”不是当前抓取器只抓到 1 条，而是这些源在当前主键下残留了历史少量记录。重刷后 `路口大爷聊宏观 / 巫师财经 / 何佳心今天加薪了么 / 铁锤观察室 / 土豆看财报` 均已补齐到 12 条。

## 2026-05-14 低条目源全量巡检
- 需求：继续排查所有启用源中条目数低于 10 的情况，区分异常漏抓与正常低频更新。
- 处理：对低条目源逐个复核页面统计、数据库记录与实时抓取结果，并对 `投研部杨摸鱼` 执行定向删库重刷。
- 结果：`投研部杨摸鱼` 已从低条目异常补齐到 12 条；当前低于 10 条的仅剩 `HackerNews 每日摘要 / 肖恩周刊 / 阮一峰周刊`，属于原生 RSS 周刊/聚合源近期本身更新不足，不是抓取异常。

## 2026-05-14 订阅内容时间列修复
- 需求：修复订阅内容列表中抖音时间看起来全是同一天、无法分辨真实发布时间的问题。
- 处理：核对数据库后确认抖音 `published_at` 已写入真实到分钟的时间；问题出在列表模板把 `display_time` 截成了前 10 位，仅显示日期。现已改为显示完整 `YYYY/MM/DD HH:MM`，并同步放宽时间列宽度。
- 结果：抖音及其他渠道的订阅内容列表均可直接显示完整发布时间，不再误判为“时间不对”。

## 2026-05-14 抖音错误日期二次校正
- 需求：用户指出抖音列表日期本身也不对，并给出具体样例 `7636052213319683379`，真实发布时间应为 `2026-05-04 23:02`。
- 处理：直接反查数据库、复核抖音主页接口返回的 `create_time`，确认抓取器当前已能解析出正确时间；问题在于库中仍残留旧错误记录。已恢复列表仅显示日期的原样式，并按 3 个抖音主页 URL 定向清库重刷。
- 结果：样例视频现已校正为 `2026/05/04 23:02`；`破壁狗 / 江平舟 / 黑猫星球` 三个抖音源均已用当前正确时间结果重新入库。

## 2026-05-13 自动运行时点调整
- 需求：改为每天 06:00 自动跑“订阅 + 稍后读”，每天 17:00 自动跑“订阅”；若服务启动时已错过当天计划点，则立刻补跑一次，后续仍按原时点执行。
- 处理：新增 Web 生命周期自动调度器，运行态持久化到 `runtime/health/automation_runtime.json`，避免当天重复重启导致同一计划点反复补跑。

## 2026-05-13 主数据分层收尾
- 进度：清除 `rss_sources.json` 中展示字段残留、清除健康状态中的主数据镜像，并删除 `rss_source_state.enabled` 影子代码。

## 2026-05-13 订阅名称 SSOT 收口
- 进度：以 `config/rss_sources.json` 为唯一主数据；移除视图层按名称补 `id` 的旧逻辑；编辑/启停/删除改为对原始配置就地更新。
- 难点与方案：保留“新增源生成一次 `id`”，但禁止读取阶段再从 `name` 派生身份；用临时改名回写验证“编辑后不再被旧别名回写”。

## 2026-05-13 顶部新闻模块
- 进度：在首页标题下新增“新闻模块”，首条接入 `alpha派蓝宝书` 外链并新窗口打开。

## 2026-05-13 新闻模块留白与标题收口
- 进度：将标题由“新闻模块”收口为“新闻”，并增加模块底部留白，拉开与下方主内容区的间距。

## 2026-05-13 稍后处理分栏拖拽
- 进度：删除右侧折叠按钮，改为拖拽分隔条；宽度写入本地存储并在刷新后恢复。

## 2026-05-13 一键重启批处理稳态修复
- 需求：修正 `scripts/一键重新拉起InfoFlowHub服务.bat`，确保双击或从任意目录调用都可正常重启服务。
- 处理：改为显式定位系统 PowerShell、使用批处理绝对脚本目录调用 `restart_infoflow_web.ps1`，并透传退出码。
- 结果：已实测批处理返回 `0`，服务首页 `http://127.0.0.1:18421/` 正常返回 `200`。

## 2026-05-13 订阅内容未读筛选翻页失效修复
- 需求：默认“未读”筛选在翻到下一页再返回上一页后失效，已读内容重新出现。
- 处理：将未读筛选状态收敛到前端单一控制器，并在每次列表片段刷新后重新应用到当前页内容。
- 结果：翻页、搜索、排序后的订阅列表都会继续保持当前未读筛选状态。
## 2026-05-13 微信公众号 connector 渠道接入
- 需求：仅参考外部项目的最小技术切片，将微信公众号作为本仓库新的 connector 渠道接入订阅内容区。
- 处理：新增 wechat connector，接入 source 识别、抓取路由、登录态校验与渠道展示；不引入外部 UI、数据库或独立 RSS 服务。
- 结果：目标是保留现有订阅主链路与主数据结构，仅增加公众号渠道能力。

## 2026-05-14 X 抓取与错误判定排查
- 进度：复现后确认 `supertechfans` 正常、`x.com/MacroMargin` 直连正常、`rsshub.app/twitter/user/MacroMargin` 当前真实返回为 `404`，截图中的 SSL/连接关闭并非唯一现态。
- 处理：将 X 抓取启动策略改为优先复用本机 Chrome 通道再回退默认 Chromium，并统一 MacroMargin 登录提示文案，减少 `ERR_CONNECTION_CLOSED` 类兼容性误报。
## 2026-05-14 微信公众号扫码登录桥
- 需求：将公众号登录态获取收口到当前站点，尽量把用户动作压缩为“打开本地页扫码一次”。
- 进度：公众号订阅已收口为 10 个，并统一名称格式为“观点 公众号名”，避免设置页出现 `??` 乱码名。
- 进度：补齐订阅内容列表的来源名映射，页面优先显示当前配置名，不再被历史入库旧名称卡住。
- 进度：将公众号半自动续期挂到设置页渠道行，支持查看登录态、点“续期”扫码、成功后自动回到设置页。
- 进度：批量删除所有订阅源名称中的“的 bilibili 动态”，收口为更短的来源名。

## 2026-05-14 首页抓取状态与右侧搜索框收紧
- 需求：首页为“订阅内容”补一个可复用的抓取状态“几/几”，并减少右侧搜索框宽度。
- 处理：复用设置页现有运行态中的 `last_success_sources/last_total_sources`，将状态胶囊放到左侧“订阅内容”标题行的抓取按钮后；桌面端右侧搜索框由 240px 收紧到 210px，移动端保持原有回落策略。
- 处理：准备接入本地 wechat-login 页面与代理登录流程，登录成功后把 token/cookie 写入 runtime/wechat_auth.json。
- 结果：目标是后续直接在当前系统内完成扫码，再继续订阅指定公众号（先处理胖特工）。

## 2026-05-14 右侧搜索框继续压缩
- 需求：将“稍后处理”顶部搜索框进一步缩小到当前宽度的三分之一。
- 处理：桌面端 `.laterhub-search-input` 宽度由 210px 调整为 70px；移动端仍保持 100% 回落，不改窄屏交互。

## 2026-05-14 右侧搜索框压到 20px
- 需求：继续把“稍后处理”顶部搜索框桌面端宽度改到 20px。
- 处理：桌面端 `.laterhub-search-input` 宽度由 70px 调整为 20px；移动端仍保持窄屏时自动占满整行。

## 2026-05-14 静态样式缓存绕过
- 现象：样式代码已改，但当前页面仍显示旧宽度。
- 处理：为 `/static/styles.css` 与 `/static/app.js` 增加版本戳查询参数，强制浏览器刷新静态资源缓存。

## 2026-05-14 右侧搜索框改到 50px
- 需求：将“稍后处理”顶部搜索框桌面端宽度调整为 50px。
- 处理：桌面端 `.laterhub-search-input` 宽度由 20px 调整为 50px，并同步更新静态资源版本戳，确保当前页刷新后立即生效。

## 2026-05-14 右侧搜索框改到 100px
- 需求：将“稍后处理”顶部搜索框桌面端宽度调整为 100px。
- 处理：桌面端 `.laterhub-search-input` 宽度由 50px 调整为 100px，并同步更新静态资源版本戳，确保当前页刷新后立即生效。

## 2026-05-14 订阅内容搜索区字体缩小
- 需求：将“订阅内容”区域截图中的搜索控件字体缩小一号。
- 处理：仅下调左侧搜索输入框、搜索按钮与“默认未读”按钮字号到 13px，不影响右侧“稍后处理”控件；同步更新静态资源版本戳，确保刷新后立即生效。

## 2026-05-14 成功源数展示收口为 SSOT
- 需求：首页“抓取状态”与设置页“成功源数”保持同一口径、同一展示来源。
- 处理：新增统一格式化函数 `format_success_sources_text`；设置页与首页都复用同一个 `success_sources_text`，不再在首页单独拼接一份展示文本；同步更新静态资源版本戳。

## 2026-05-14 成功源数空白修复
- 现象：运行态与后端视图中已有 `28/28`，但页面“成功源数”位置渲染为空白。
- 处理：模板读取改为显式按键取值 `status['success_sources_text']`，避免点号解析导致的空渲染；同步更新静态资源版本戳。

## 2026-05-14 新闻链接居中
- 需求：首页新闻区中的 `Alpha派 蓝宝书` 链接改为居中显示。
- 处理：将 `.news-links` 增加 `justify-content: center`，只调整链接行对齐方式；同步更新静态资源版本戳。

## 2026-05-14 订阅源管理横向滚动

## 2026-05-16 订阅内容未读颜色修复
- 需求：用户反馈订阅列表里未读内容被显示成灰色，视觉上像已读。
- 处理：将订阅条目的阅读态样式改为显式双态，模板默认渲染为 `cell-unread`，前端统一通过 `setEntryReadState` 在 `cell-unread / cell-read` 间切换，不再复用易串色的默认粗体类。
- 结果：未读条目恢复为正文色并加重显示，已读条目继续保持灰色，且不影响现有“默认未读”筛选行为。

## 2026-05-16 B站动态链接稳态修复
- 需求：用户指出个别 B 站动态条目虽然有链接，但实际打开不稳定。
- 处理：在订阅展示层统一将 `t.bilibili.com/{动态ID}` 改写为 `https://www.bilibili.com/opus/{动态ID}`，避免旧动态短链在部分环境里空白或打不开。
- 结果：现有库内旧记录无需重抓即可立即使用更稳的 `opus` 链接。

## 2026-05-16 订阅分页被重置修复
- 需求：用户点击“下一页”后无法停留在目标页，列表会被脚本重新打回第一页。
- 处理：仅调整 `setupUnreadToggle`，改为从当前片段回读未读状态，不再在片段刷新后主动执行 `refreshEntriesPanel({ entries_page: "1" })`。
- 结果：分页切换不再被前端二次刷新覆盖，保留现有未读筛选按钮行为。

## 2026-05-16 首次打开默认未读回补
- 需求：用户反馈首次打开页面时，“默认未读”按钮高亮但列表未按本地已读键过滤。
- 处理：仅在 `setupUnreadToggle` 回补首次同步逻辑；当片段里的 `unread/read_keys` 与前端本地状态不一致时，按当前页重新拉取，而不是强制退回第一页。
- 结果：首次打开可立即按“默认未读”口径过滤，同时不破坏分页停留。

## 2026-05-16 首页立即抓取回填目标修复
- 需求：首页“立即抓取”点击后看起来无效果，而设置页同按钮正常。
- 处理：仅修改首页模板，将按钮的 `hx-target` 从设置页才存在的 `#runtime-status-panel` 改为首页实际存在的 `#entries-fetch-progress`，并让抓取返回用 `outerHTML` 刷新该状态胶囊。
- 结果：首页点击“立即抓取”后可直接看到抓取状态更新，不再因目标节点不存在而无感。

## 2026-05-16 设置页运行状态区重排
- 需求：状态卡改为更紧凑的单行视觉；“新增条目”明确为每轮抓取新增数；底部改成“运行日志 / 错误摘要”双列固定高度滚动区。
- 处理：设置页状态数据补充 `last_run_inserted_entries` 与 `run_log_text`；状态卡改为紧凑字号；错误区改为双列滚动面板。
- 结果：设置页运行状态区信息更集中，新增条目口径收口为本轮新增，日志与错误可并排查看。

## 2026-05-16 设置页标题行收口
- 需求：用户希望“订阅设置”和“立即抓取”保持同一行。
- 处理：仅为设置页该标题行增加专用 nowrap 类，禁止这一行在当前桌面宽度下换行。
- 结果：设置页标题与按钮保持单行横排，不影响其他面板标题。

## 2026-05-16 设置页区域合并
- 需求：用户希望将“订阅设置运行状态区”和“订阅源管理”合并成一个区域。
- 处理：仅调整设置页模板结构，将 `sources-panel` 移入“订阅设置”卡片内部，并补一段内部间距样式。
- 结果：设置页上半部分不再拆成两张独立卡片，而是合并为同一区域展示。

## 2026-05-16 设置页宽度自适应
- 需求：设置界面整体偏宽，浏览器需要整页横向滑动，希望按百分比/弹性布局适配窗口。
- 处理：仅调整样式层，将主容器、设置页卡片、状态卡、日志区、搜索工具栏与订阅源表格列宽改为更弹性的百分比/自适应网格；极窄宽度下只保留表格局部横向滚动。
- 结果：设置页在常见浏览器宽度下优先整体适配，不再整页横向拖动；必要时仅表格区局部滚动。

## 2026-05-16 订阅源状态列合并进操作列
- 需求：用户希望把表格里的“生效/停用”状态列去掉，避免和操作列里的启用/停用按钮重复。
- 处理：仅删除订阅源表格中的“生效”列，并回收列宽给渠道、来源类型、最近错误与操作列。
- 结果：状态表达收口到操作列，表格信息密度更高。

## 2026-05-16 订阅源启停按钮回迁
- 需求：用户纠正需求，应保留“生效”列，并把启用/停用按钮放到这一列里；操作列仅保留编辑与删除。
- 处理：恢复“生效”列与列宽，并把启用/停用表单从操作列移回该列。
- 结果：表格语义与操作分工恢复清晰，启停与编辑删除分列展示。
## 2026-05-16 订阅内容链接兜底修复
- 进度：修复首页订阅条目“看起来可点但无跳转”的问题；后端统一校验条目链接，缺失时回退到来源主页；前端对无效链接显示禁用态。
- 难点与方案：问题点不稳定，先在展示层收口为“只渲染合法绝对链接”，避免脏数据继续透出成假链接；同步更新静态资源版本戳，确保刷新即生效。
- 需求：订阅源管理表底部增加横向滑动能力，避免右侧列显示不全。
- 处理：为表格容器显式保留横向滚动槽，并为 `.sources-table` 设置最小总宽度 `1560px`，让底部稳定出现横向滚动条；同步更新静态资源版本戳。

## 2026-05-14 订阅源表横向滚动修正
- 现象：仅设最小宽度后，表格仍可能被 `width: 100%` 压缩，横向滚动条不出现。
- 处理：将 `.sources-table` 改为 `width: max-content; min-width: 100%`，让表格按内容真实宽度展开，超出容器时稳定触发底部横向滚动；同步更新静态资源版本戳。

## 2026-05-14 YouTube 改为网页唯一事实源
- 需求：YouTube 不再走 RSS，避免网页、数据库与时间口径不一致。
- 处理：将 YouTube 源主数据切换为 `provider=web`，抓取主路径改为网页直抓；补充相对时间归一逻辑，清理旧库后按网页结果重抓。
- 进度：进一步收紧为“拿不到单视频链接就不入库”，已清空 `老厉害` 历史 YouTube 旧记录；当前频道页可访问，但尚未稳定解析出单视频卡片，因此暂时宁可空库，也不再写入频道页假链接与假时间。
- 进度：将 `老厉害` 主键改为纯 ASCII `laolihai_youtube`，准备同步迁移数据库与运行态健康键，并只保留网页 Latest 最新 12 条。

## 2026-05-14 订阅主配置改名
- 需求：去掉 `rss_sources.json` 历史命名包袱，改成能覆盖网页源/公众号/X/YouTube 的统一订阅配置名。
- 处理：主配置改名为 `subscription_sources.json`，并同步调整读写入口、初始化模板与修复脚本引用。

## 2026-05-14 抖音订阅渠道接入
- 需求：在 `subscriptions/connect` 增加“抖音订阅”，并复用现有抖音登录态，不新增第二套 cookies。
- 处理：新增抖音主页订阅抓取器；在网页直抓路由中加入 `douyin.com/user/...` 识别与分发；默认认证元数据新增 `douyin_profile/PW_DOUYIN_PROFILE`；登录脚本改为先打开抖音首页再跳收藏页；主配置补入 3 个抖音订阅源。
- 结果：抖音收藏继续留在 laterhub，抖音订阅进入 subscriptions 主链，两者共享同一份抖音 profile。

## 2026-05-14 抖音订阅源命名收口
- 需求：将 3 个抖音订阅源名称改为“观点/军事 + up 名”，并把 `id` 直接改成 up 名。
- 处理：直接更新主配置中的 3 条抖音源；同时核对当前无历史订阅入库记录、无健康状态键依赖，无需额外迁移。

## 2026-05-14 旧抖音登录态目录清理
- 需求：删除历史遗留的 `runtime/browser_profiles/pw-douyin-profile`，避免与当前 `douyin_shared` 混淆成两套抖音登录态。
- 处理：核对代码层已无旧目录引用后，删除旧目录，仅保留 `runtime/browser_profiles/douyin-shared` 作为唯一抖音共享登录态。

## 2026-05-14 单账号统一登录态收口
- 总需求与计划：为 laterhub 与 subscriptions 建立统一认证资产层，以 `auth_key` 作为单一入口，先完成微信、抖音、B站、X、微博五类登录态的注册、校验、展示与兼容迁移。
- 进度：开始落认证注册表、provider、设置页认证资产面板，并同步给订阅源配置补 `auth_key`。

## 2026-05-14 设置页认证资产按钮与抖音占位名修复
- 需求：修复认证资产操作列按钮被压成竖排，以及抖音订阅源名称显示为 `??` 的表现问题。
- 处理：放宽认证资产表操作列宽度并强制按钮不换行；将 3 条抖音订阅源占位 `id/name/note` 修正为可读主数据。
- 结果：设置页操作按钮可保持单行显示，订阅源列表不再出现抖音 `??` 名称。

## 2026-05-15 06:00 自动订阅大面积失败排查与兜底
- 总需求与计划：从 `runtime/health/automation_runtime.json` 与 `subscriptions_source_health.json` 倒查 2026-05-14 17:00 正常、2026-05-15 06:00 变成 `0/29` 的原因，优先收敛公共根因，补最小兜底并完成一次可验证回归。
- 排查结论：06:00 同窗同时出现 `SSL EOF`、`ConnectionResetError 10054`、`ERR_CONNECTION_CLOSED`，更像跨平台瞬时网络/连接层抖动，而非 B 站、公众号、抖音、X、YouTube 同时规则变更；同时原运行态对失败过于敏感，少量异常会把整轮观感放大成接近全挂。
- 处理：为网页源与微信公众号请求补轻量重试；收紧订阅运行态成功判定，登录态依赖继续记警告，但不再把已成功的大多数源误写成 `0/N` 级别观感。
- 回归结果：2026-05-15 15:05:03 手动复跑后，`last_success_sources=27`、`last_total_sources=29`、`last_inserted_entries=524`、`fetch_state=success`；剩余 2 个失败源均为抖音登录态失效，不再是系统性抓取故障。

## 2026-05-15 ??????????
- ??????????????????`???` ???????? `???? + aweme_id`?
- ??????????????????????? `aweme_list` ?????????????????????????????? 3 ?????
- ???????????????? 3 ????????????? `???` ???? `???? + aweme_id` ?????

## 2026-05-15 ??? YouTube ??????
- ??????? `???` ?????????????????????????
- ??????? YouTube ???????????????????????????????? `laolihai_youtube` ???????
- ???YouTube ?????????? `17h ago / 2w ago` ???????????? `17 hours ago / 2 weeks ago`?????????????
- ???????????????? `laolihai_youtube`???????????????

## 2026-05-15 ???????????
- ??????????????????????
- ???????????????? `wechat-finninvestrecord` ????????????????????????????? `(source_id, link)`?
- ?????????????????? + ????????????????????????????

## 2026-05-15 ?????????????
- ?????????????????????????????????
- ?????????????????????????????? `link` ????????????????????? upsert ???? `(source_id, link)` ??? `link`?


## 2026-05-15 订阅主数据最终收口
- 总需求与计划：将订阅主数据彻底收口到 config/subscription_sources.json，清除旧 rss_sources 命名和运行态主数据影子。
- 进度：已收紧配置结构校验；健康状态示例与写入逻辑仅保留运行态字段；设置页读取改为只消费规范主配置和运行态最小字段。

## 2026-05-15 laterhub ?? 502 ????
- ?????????? laterhub ?? LLM 502 ?????????????? pending ????????????????????????????????????????

## 2026-05-15 未读改为先筛后分页
- 需求：默认未读时先按已读口径过滤，再做分页补位；并将 2026-05-01 之前的订阅条目统一视为已读。
- 处理：左侧列表改为后端未读过滤后再分页；前端将用户已读链接随请求透传，并自动把 2026-05-01 之前的历史条目标记为已读。

## 2026-05-15 未读默认态与静态缓存修复
- 需求：用户刷新首页后仍看不到未读分页修复结果。
- 处理：首页首屏渲染默认带上 `entries_unread_only=1`，并更新静态资源版本号，避免浏览器继续使用旧脚本缓存。

## 2026-05-15 已读缓存迁移
- 需求：旧浏览器本地已读缓存污染了当前未读结果，导致 2026-05-01 之后的新条目也被误判为已读。
- 处理：切换订阅读缓存键到新版本，仅沿用新规则重新积累已读状态；历史 2026-05-01 之前条目仍会自动标记为已读。

## 2026-05-15 MacroMargin 标题纠偏
- 需求：https://x.com/MacroMargin/status/2053834275775201622 的标题被错误写成分隔符。
- 处理：已直接回填该条数据库标题，并修正 X 文章标题解析规则。

## 2026-05-16 未读键碰撞修复
- 需求：列表中可见的未读条目，在“默认未读”筛选下被错误排除。
- 处理：将未读筛选使用的链接键从可碰撞的截断前缀改为稳定长哈希，并升级浏览器本地缓存版本，清除旧错键影响。

## 2026-05-16 未读前后端键算法对齐
- 需求：默认未读首屏仍展示全量内容，说明前后端未读键未真正匹配。
- 处理：前端改为与后端一致的 SHA-1 链接键；同时在首屏对比已读键快照，不一致时立即自动刷新未读列表。

## 2026-05-16 首页抓取按钮视觉对齐
- 需求：将“订阅内容”区域抓取按钮与“订阅设置”区域保持同一视觉效果与文案口径。
- 处理：统一首页左侧按钮文案为“立即抓取”，直接复用现有主按钮样式，不再保留同页“抓取/立即抓取”双口径。

## 2026-05-16 中文乱码修复与 UTF-8 收口
- 需求：修复设置页运行态文案、微信公众号认证状态文本、`subscription_sources` 配置相关乱码，并收紧后续读写链路编码。
- 处理：确认污染点落在设置页视图聚合层、网页直抓兜底文案、正式订阅配置注释，以及认证/运行态/订阅配置 JSON 入口；新增统一 UTF-8 JSON 读写工具并接入这些链路。
- 结果：相关显示文案与配置文本已恢复正常中文，后续 JSON 统一按 UTF-8 读写，兼容 BOM 输入并固定无 BOM UTF-8 输出。

## 2026-05-16 设置页认证资产置顶
- 需求：将“认证资产”从订阅源管理内部拆出，作为设置页最上方的独立区域展示。
- 处理：抽出独立局部模板 `auth_assets_panel`，在设置页顶部单独渲染；原订阅源管理区移除重复表格，保持单一渲染来源。
- 结果：设置页打开后先看到完整认证资产区域，订阅源管理仅保留订阅源相关内容。

## 2026-05-16 订阅源管理表格紧凑化
- 需求：订阅源管理与上方日志区拉开间距，整体更紧凑，字体缩小一号，每个订阅源保持单行不换行。
- 处理：收紧表格列宽、单元格留白与按钮尺寸，日志区下方增加留白；认证资产与认证状态改为同排展示，操作按钮在窄屏下也保持单行。

## 2026-05-16 设置页日志区下方留白加大
- 需求：运行日志/错误摘要下方与订阅源管理之间的距离再拉开一点。
- 处理：仅增加设置页日志区块后到订阅源管理区块的顶部间距，不调整其他布局参数。

## 2026-05-16 稍后处理多标签修复
- 进度：修复 `A:B` 组合标签被误当单标签的问题；统一按多标签拆分、展示与筛选。

## 2026-05-16 标签提示词收紧
- 进度：重写 `laterhub` 标签器提示词，补齐白名单、分类边界、多标签门槛、冲突约束与兜底规则；同时统一标签输出为标准逗号分隔。

## 2026-05-16 标签 few-shot 示例补齐
- 进度：在标签提示词中加入 12 组正例与 6 组反例，覆盖心理类边界、AI/效率工具、娱乐新闻/作品内容、文学解读等高频误判场景。

## 2026-05-17 laterhub LLM 502 韧性修复
- 进度：修复 `apps/laterhub/tagger.py` 与 `apps/laterhub/pipeline.py` 在 429/502/503/504、超时、连接波动下的状态流转；临时波动改为单条降级或保留 `pending` 待重试，不再把整批卡死。
- 变更：标签阶段拆分“临时波动/真实失败”并接入主备 LLM 顺序降级；推送阶段新增 `mark_pending_retry`，临时上游异常不再直接落 `failed`；日志新增 `[临时波动] / [真实失败]` 清晰标识。
- 最小验证：先做语法编译通过，再用 monkeypatch 分别模拟标签 502 和推送 503，观察单条降级、`pending` 保留与日志分类是否符合预期。

## 2026-05-17 微信公众号认证过期提示补齐
- 进度：补齐设置页认证资产区的剩余时间、24 小时预警、过期续期入口与登录页中文状态提示。
- 变更：统一修正 `wechat_mp_main` 相关 UTF-8 文案；登录成功后改为按 `expire_time` 单一口径展示，并补充异常与边界日志。

## 2026-05-17 微信公众号免扫码续期验证
- 进度：实测确认现有有效 cookie/token 请求 `mp.weixin.qq.com/cgi-bin/home` 时会返回新的 `Set-Cookie` 与更晚的 `Expires`，可用于静默续期。
- 变更：新增“免扫码续期”链路，优先按微信响应头中的真实 cookie 过期时间回写 `runtime/wechat_auth.json`；仅在现有登录态已失效时回退到重新扫码。

## 2026-05-17 微信公众号抓取前自动续期
- 进度：将自动续期触发点收口到“真正抓取公众号前”，仅当距离当前截止时间不超过 3 天时尝试一次免扫码续期。
- 变更：不做每天定时任务；若 3 天窗口内当日未发生公众号抓取，则不触发，等下一次实际抓取前再判断。

## 2026-05-18 微信扫码后仍显示过期修复
- 进度：修正扫码完成与免扫码续期的有效期写回口径，改为以真实微信接口返回的未来 cookie 过期时间为准。
- 难点与方案：定位到 `ret=0` 被错误判成失败，导致有效会话未刷新 `expire_time`；修复后已将当前公众号登录态续期回写到 2026-05-22 09:13。

## 2026-05-18 微信认证资产与订阅源列表口径对齐
- 进度：修正设置页“认证资产可用、下方公众号源仍显示登录已过期”的错位展示。
- 难点与方案：确认下方显示的是历史运行态错误残留，不是当前认证资产；现已对公众号源优先按当前认证状态渲染，并清理旧的过期错误残留。

## 2026-05-18 生活精选君 fakeid 固化
- 进度：修复“生活 精选君”单源抓取失败。
- 难点与方案：定位到该源仍依赖文章链接临时反推 `fakeid`，现已固化为 `wechat://mp/MjM5NjMzODAyMA==`，单源验证已恢复正常抓取。

## 2026-05-20 稍后处理标签改为单选
- 进度：将右侧标签筛选从多选切换为单选；点击新标签直接覆盖当前筛选，再点当前标签可取消回到“全部”。
- 难点与方案：后端过滤兼容旧的逗号参数，只取首个标签作为唯一筛选值，避免历史链接或缓存参数导致异常。

## 2026-05-20 laterhub 人工处理流减负
- 进度：为 laterhub 补齐“本轮新增 / 今日新增”快速过滤、当前筛选结果批量完成，以及单条完成后保留当前筛选与页码的上下文交互。
- 变更：后端将 laterhub 筛选口径抽为统一查询层；“本轮新增”按最新 `created_at` 分钟桶识别；新增批量完成接口，直接作用于当前筛选结果中的未完成项。

## 2026-05-20 隐藏已选标签展示
- 进度：移除稍后处理筛选统计中的“已选标签”展示，仅保留筛选后条目数，减少界面噪音。

## 2026-05-20 laterhub 重复出现条目自动回退未处理
- 进度：已将 laterhub 已完成条目在后续抓取中再次出现时，自动从“已完成”回退为“未处理”。

## 2026-05-20 laterhub 人工完成按钮移除与状态清零
- 进度：移除右侧“全部积压 / 本轮新增 / 今日新增 / 完成当前筛选结果”等人工处理按钮，并将现有条目统一回退为未完成。

## 2026-05-20 laterhub 已打开状态接入
- 进度：为稍后处理新增“已打开”状态；点击链接即记录已打开并灰显；状态筛选新增“已打开”；历史已完成条目同步补记为已打开。

## 2026-05-20 runtime UTF-8 收口补全
- 进度：统一 `runtime/health/*.json`、`runtime/wechat_auth.json`、`runtime/auth/wechat_mp_main.json` 的读写口径为 UTF-8 无 BOM，并补历史乱码自愈。
- 难点与方案：确认本次残留问题主要是历史 mojibake 数据而非文件编码本身；新增共享文本规范化层，统一修复微信昵称、定时任务 label、source health 名称/错误文案，并用脚本将当前运行态文件按新口径重写验证。

## 2026-05-20 订阅源健康态旧 source id 清理
- 进度：排查确认 `wechat-kindle-jingxuanjun` 为历史残留旧键，当前 canonical id 为 `wechat-jingxuanjun`；污染点落在订阅历史库与运行时健康 JSON。
- 处理：新增 source id 别名映射，统一在健康态读写、订阅统计、删除清理与历史入库迁移时归并到 canonical id。
- 结果：目标是设置页与健康统计只保留当前 canonical source id，旧键不再继续回流。

## 2026-05-20 自动任务时点收口
- 需求：将定时部分改为每天上午 06:00，延迟后自动运行，并保证每天只自动跑一次。
- 处理：自动调度器移除原 `17:00` 档，仅保留 `06:00` 主计划；新增固定延迟窗口后再执行；启动时同步清理旧的 `daily_1700` 运行态残留。
